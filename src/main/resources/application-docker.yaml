# server.name=mukul
spring:
  #
  application:
    name: tickets
  #
  datasource:
    url: jdbc:postgresql://ticket-database:5432/ticket-service
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: daroch
  #
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
